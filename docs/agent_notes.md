# Agent Notes

- No prior .codex notes were found for this repository.
- Use `contact_reports.english_content` as the fresh extraction corpus; ignore legacy `predictions` JSON when planning the audit pipeline.
- Add new audit tables with a `prediction_audit_` prefix instead of mutating legacy JSON columns on `contact_reports`.
- Keep one row per extracted future claim in `prediction_audit_predictions`; store run/version metadata separately in `prediction_audit_runs`.
- Validate new schema with `psql "$DatabaseURL" -v ON_ERROR_STOP=1 -f sql/20260311_prediction_audit_schema.sql` and catalog queries for tables, constraints, and indexes.
- Seed Stage 1 with `python3 scripts/parse_contact_report_predictions.py`; current full-corpus baseline is run `stage1-20260310T225611Z` with `6200` candidates across `873` reports.
- Keep Stage 1 recall-oriented but add disqualifiers for conversational future tense (`I will tell you`, `we will meet`) before inserting rows.
- Add `last_stage2_run_id`, `stage2_reviewed_at`, and `stage2_meta` to prediction rows so rerun provenance is queryable per row.
- Run Stage 2 with `python3 scripts/review_prediction_candidates.py --parse-run-key stage1-20260310T225611Z --only-pending`; initial full pass is `stage2-20260310T230558Z`.
- Expect initial Stage 2 to be conservative on duplicates; first run produced `226` significant, `1031` eligible, and `0` duplicate collapses.
- Build the first Stage 3 ledger family-by-family; earthquakes now use `python3 scripts/build_earthquake_event_ledger.py --stage2-run-key stage2-20260310T230558Z`.
- Prefer a local location override map for batch target resolution instead of a public geocoder; current file is `data/earthquake_location_overrides.json`.
- Bound the first earthquake ledger run to rows with normalized time windows and skip compound multi-event claims explicitly; live run `stage3-earthquake-20260310T231544Z` inserted `281` USGS candidate events from `17` resolved predictions.
- Represent compound multi-event predictions explicitly with `prediction_audit_bundles` plus child linkage columns on prediction rows; do not skip them as unmodelled edge cases.
- Full bundle-aware reruns are `stage1-20260310T232924Z` and `stage2-20260310T232950Z`; they produced `22` bundle rows and `5` fully significant bundles.
- Rerun the earthquake ledger after bundle extraction changes; `stage3-earthquake-20260310T233040Z` resolved `23` predictions, skipped only `1` remaining compound row, and inserted `284` USGS candidate events.
- Score earthquake Stage 4 with `python3 scripts/score_earthquake_matches.py --stage2-run-key stage2-20260310T232950Z --stage3-run-key stage3-earthquake-20260310T233040Z`; live run is `stage4-earthquake-20260310T234614Z`.
- Current earthquake Stage 4 outcome counts are `14 exact_hit`, `4 near_hit`, `2 similar_only`, `3 miss`, `9 unresolved` across `32` scoped predictions.
- Bundle-level Stage 4 rollups now live on `prediction_audit_bundles`; current full parse run has `1 exact_hit` bundle, `7 unresolved`, and `14 unreviewed` pending other families.
- Assign earthquake Stage 5 probabilities with `python3 scripts/assign_earthquake_probabilities.py --stage2-run-key stage2-20260310T232950Z --stage4-run-key stage4-earthquake-20260310T234614Z`; live run is `stage5-earthquake-20260310T235515Z`.
- Store mutually exclusive per-row outcome probabilities that sum to 1: `p_exact_under_null`, `p_near_under_null`, `p_similar_under_null`, `p_miss_under_null`.
- Keep Poisson rate inputs in `probability_meta` (`event_counts`, `annual_rates`, `lambda`, `window_days`) so downstream Python can recompute or combine probabilities without reverse engineering the model.
- Rebuild-safe earthquake Stage 3 needs explicit match-status selection; use `--match-statuses all` after new target overrides because the default `unreviewed` filter skips already-scored rows.
- Keep `Loyalty Islands` in `data/earthquake_location_overrides.json`; the override moved one earthquake row from unresolved to `near_hit` on rerun.
- Latest full earthquake reruns are `stage3-earthquake-20260311T005707Z`, `stage4-earthquake-20260311T005940Z`, and `stage5-earthquake-20260311T005944Z`.
- Export earthquake artifacts with `python3 scripts/export_earthquake_analysis.py --stage5-run-key stage5-earthquake-20260311T005944Z`; current outputs land in `data/exports/earthquake/stage5-earthquake-20260311T005944Z/`.
- Use export summaries in log space for combination work; current earthquake observed-outcome aggregate is `log10_sum = -35.740008` across `24` probability-ready rows.
- Use `data/earthquake_prediction_overrides.json` for context-backed target fixes keyed by `report_number:candidate_seq`; current overrides resolved Tokyo, Erzincan, Grisons-Rhine, and Onagawa earthquake rows without changing Stage 1 parsing.
- Latest earthquake reruns after prediction overrides are `stage3-earthquake-20260311T010726Z`, `stage4-earthquake-20260311T010820Z`, and `stage5-earthquake-20260311T010829Z`; scoped results are `15 exact_hit`, `7 near_hit`, `2 similar_only`, `4 miss`, `4 unresolved`.
- Remaining earthquake unresolveds are now the hard cases only: reports `235/14`, `383/23`, `400/16`, and `779/483`.
- Bundle probabilities now live in `public.prediction_audit_bundle_rollups`; apply `sql/20260311_prediction_audit_bundle_probability_rollups.sql` before running `python3 scripts/rollup_bundle_probabilities.py`.
- Scope Stage 6 bundle rollups to the same time-windowed cohort as Stage 3/4/5; otherwise bundle counts drift above the exported earthquake set.
- Latest earthquake bundle rollup run is `stage6-bundle-rollup-20260311T010947Z`; scoped bundle results are `3 exact_hit` and `3 near_hit`, all probability-ready.
- Latest earthquake export after Stage 6 is `data/exports/earthquake/stage5-earthquake-20260311T010829Z/`; current observed-outcome aggregate is `log10_sum = -39.491954` across `28` probability-ready rows.
- Only sweep unrelated repo dirt into a commit when the user explicitly asks for a full-repo commit; otherwise keep prediction-audit bundles isolated.
- Finalize earthquake cohort membership with `sql/20260311_prediction_audit_final_reviews.sql` plus `python3 scripts/finalize_earthquake_predictions.py --stage5-run-key stage5-earthquake-20260311T010829Z`; latest final run is `stage7-earthquake-final-20260311T024920Z`.
- Stage 7 currently yields `28 included_in_statistics`, `3 excluded_from_statistics`, and `1 permanently_unresolved` across the scoped earthquake cohort.
- Keep manual final-review rules in `data/earthquake_final_adjudications.json` keyed by `report_number:candidate_seq`; require every unresolved earthquake row to be covered before Stage 7 runs.
- The Stage 7 script uses regular psycopg2 cursors; convert fetched tuples to dicts from `cur.description` before treating them like mapping rows.
- Refresh the Stage 5 export after Stage 7 so `summary.json`, `predictions.csv`, and `unresolved.csv` carry `final_status`, `final_reason`, and `stage7_run_key`.
- Start the epidemic family with a narrow named-disease slice before attempting the full cohort; current curated files are `data/epidemic_prediction_overrides.json` and `data/epidemic_official_events.json`.
- Use `python3 scripts/build_epidemic_event_ledger.py --stage2-run-key stage2-20260310T232950Z` for the first epidemic Stage 3 slice; live run is `stage3-epidemic-20260311T030220Z`.
- Use `python3 scripts/score_epidemic_matches.py --stage2-run-key stage2-20260310T232950Z --stage3-run-key stage3-epidemic-20260311T030220Z` for the first epidemic Stage 4 slice; live run is `stage4-epidemic-20260311T030227Z`.
- The initial epidemic named-disease slice currently covers `369/12`, `401/10`, `441/1`, and `729/12` and scored `1 exact_hit`, `1 near_hit`, `2 similar_only`.
- Keep the epidemic slice source-backed and narrow; bird-flu and COVID predictions can use WHO/UK government sources, while AIDS/Corona-origin claims need separate rulebooks later.
- Export the epidemic named-disease slice with `python3 scripts/export_epidemic_analysis.py --stage4-run-key stage4-epidemic-20260311T030539Z`; current artifacts land in `data/exports/epidemic/stage4-epidemic-20260311T030539Z/`.
- Persist effective override windows in epidemic ledger `raw_event` metadata so the export can show the real Stage 3 window even when the Stage 2 row lacks normalized dates.
- The epidemic export should track the latest Stage 4 run only; discard superseded intermediate artifact directories instead of committing every transient rerun.
- Expanding the first epidemic slice with `453/14` uses the 2007 Surrey foot-and-mouth outbreak; latest runs are `stage3-epidemic-20260311T030713Z` and `stage4-epidemic-20260311T030720Z`.
- The expanded epidemic named-disease slice now covers `5` rows and scores `2 exact_hit`, `1 near_hit`, `2 similar_only`.
- Finalize the epidemic named-disease slice with `python3 scripts/finalize_epidemic_predictions.py --stage4-run-key stage4-epidemic-20260311T030720Z`; latest run is `stage7-epidemic-final-20260311T031120Z`.
- The current finalized epidemic named-disease cohort is simple: all `5` scoped rows are `included_in_statistics`, so no manual epidemic adjudication rules were needed yet.
- Refresh `data/exports/epidemic/stage4-epidemic-20260311T030720Z/` after epidemic Stage 7 so the CSV carries `final_status`, `final_reason`, and `stage7_run_key`.
- Assign epidemic Stage 5 probabilities with `python3 scripts/assign_epidemic_probabilities.py --stage4-run-key stage4-epidemic-20260311T030720Z`; latest run is `stage5-epidemic-20260311T031534Z`.
- The epidemic Stage 5 model is provisional and catalog-scoped: it uses smoothed disease-specific rates from `data/epidemic_official_events.json`, not a complete historical surveillance dataset.
- Refresh the epidemic export after Stage 5 so `summary.json` and `predictions.csv` carry `p_*`, `probability_model_version`, and the combined observed log-probability summary.
- Exclude official epidemic events dated before `claimed_contact_date` when building Stage 3 ledger rows; otherwise Stage 4 can award backward-looking `similar_only` matches.
- Temporal-guardrail epidemic reruns are `stage3-epidemic-20260311T032248Z`, `stage4-epidemic-20260311T032302Z`, `stage5-epidemic-20260311T032413Z`, and `stage7-epidemic-final-20260311T032413Z`; the current five-row slice scores `2 exact_hit`, `2 similar_only`, `1 miss` with export log10 sum `-3.677292`.
- Extend the epidemic named-disease slice with `757/1` as a `covid19` `death_threshold_crossed` row using the CDC timeline date `2021-02-21` for the United States surpassing 500,000 reported deaths.
- Latest expanded epidemic runs are `stage3-epidemic-20260311T032741Z`, `stage4-epidemic-20260311T032753Z`, `stage5-epidemic-20260311T032809Z`, and `stage7-epidemic-final-20260311T032809Z`; the six-row slice now scores `3 exact_hit`, `2 similar_only`, `1 miss` with export log10 sum `-4.756224`.
- Extend the epidemic slice with `732/13` as a `covid19` `regional_severe_outbreak_reported` row keyed to WHO Europe's 6 March 2020 Italy briefing; only score the Italy severity clause, not the broader Europe clause.
- Latest epidemic runs after adding `732/13` are `stage3-epidemic-20260311T033313Z`, `stage4-epidemic-20260311T033325Z`, `stage5-epidemic-20260311T033340Z`, and `stage7-epidemic-final-20260311T033340Z`; the seven-row slice now scores `4 exact_hit`, `2 similar_only`, `1 miss` with export log10 sum `-6.698059`.
- Add `sql/20260311_prediction_audit_provenance_support.sql` plus `scripts/seed_report_provenance.py` to capture report-level provenance evidence separately from prediction rows; do not backfill `earliest_provable_public_date` without actual public-date evidence.
- Latest provenance seed run is `stage0-provenance-20260311T033833Z`; it inserted `22` claimed-date placeholder evidence rows covering `35` included predictions and exported the manual enrichment queue to `data/exports/provenance/stage0-provenance-20260311T033833Z/`.
- Keep manual provenance evidence in `data/report_provenance_manual_evidence.json` and apply it with `python3 scripts/apply_report_provenance_evidence.py`; latest manual evidence run is `stage0-provenance-20260311T034741Z`.
- Refresh the family exports after provenance imports; the current epidemic export for `stage4-epidemic-20260311T033325Z` now shows `5` populated public dates and `4` observed-event-before-publication conflicts, while the earthquake export for `stage5-earthquake-20260311T010829Z` shows `6` populated public dates and `5` such conflicts.
- Use `python3 scripts/export_publication_timing_audit.py --stage2-run-key stage2-20260310T232950Z` for a cross-family timing check; latest audit is `data/exports/provenance/publication-timing-audit-20260311T034930Z/` with `11` populated public dates across included scored rows and `9` conflicts where the observed event predates the current provable public date.
- Add Stage 8 publication adjudication with `sql/20260311_prediction_audit_public_date_reviews.sql` and `python3 scripts/review_publication_timing.py`; latest run is `stage8-publication-20260311T045630Z`.
- Current Stage 8 status counts are `24 no_public_date_evidence`, `9 event_precedes_publication`, and `2 public_date_ok` across the `35` included scored rows; keep this separate from `final_status` so claimed-date and public-date analyses can coexist.
- Refresh `export_publication_timing_audit.py`, `export_epidemic_analysis.py`, and `export_earthquake_analysis.py` after Stage 8 so the CSVs and summaries carry `public_date_status`, `public_date_reason`, and publication timing conflict counts.
- Use `python3 scripts/export_cohort_comparison.py --stage2-run-key stage2-20260310T232950Z` to quantify the effect of public-date evidence on the statistical set; latest export is `data/exports/provenance/cohort-comparison-20260311T050004Z/`.
- Current cohort sizes are `35` for the claimed-date baseline, `26` for the public-date-not-disproven cohort, and `2` for the strict public-date-clean cohort; keep all three views available for later Python aggregation.
- The second manual evidence batch should cover reports `136`, `150`, `238`, `242`, `246`, `369`, `383`, `401`, `465`, `467`, `519`, `683`, and `725` from Future Of Mankind translation pages before rerunning Stage 8.
- Latest provenance import after the expanded manual evidence file is `stage0-provenance-20260311T050628Z`; latest Stage 8 run is `stage8-publication-20260311T050642Z`.
- After the expanded evidence batch, all `35` included scored rows now have a recorded public date; current Stage 8 counts are `30 event_precedes_publication` and `5 public_date_ok`, with no remaining `no_public_date_evidence` rows in the included scored cohort.
- Latest cohort comparison export is `data/exports/provenance/cohort-comparison-20260311T050642Z/`; current cohort sizes are `35` claimed-date baseline, `5` public-date-not-disproven, and `5` strict public-date-clean because every evidence-backed row is now either clean or a conflict.
- Export the public-date-conflict set explicitly as `public_date_excluded.csv`; the latest cohort comparison export now carries `30` excluded scored rows with combined observed log10 probability `-45.915355`.
- Add `cohort_family_summary.csv` to the cohort comparison export so downstream Python can read per-family claimed-vs-clean-vs-excluded aggregates without rebuilding subsets.
- Refresh family summaries after provenance cohort changes; earthquake and epidemic exports now expose `public_date_cohort_summary` alongside their existing claimed-date totals.
- Rank Stage 8 publication conflicts by `surprisal_log10` before doing more source research; use `python3 scripts/export_public_date_research_queue.py --stage2-run-key stage2-20260310T232950Z`.
- Latest public-date research queue export is `data/exports/provenance/public-date-research-queue-20260311T061417Z/`; current queue has `30` conflicts with combined observed log10 probability `-45.915355`.
- Top rescue targets by statistical weight are earthquake `465/6`, `442/3`, `246/20`, `401/11`, `436/3`, `136/159`, and epidemic `453/14`, `732/13`.
- Use override-driven family slices when Stage 2 leaves a measurable claim unclassified; the Kevin Rudd apology row `459/4` was lifted into a scoped `politics_election` slice without rerunning the entire corpus.
- Politics Stage 3/4/7 runs for the Kevin Rudd apology slice are `stage3-politics-20260311T064617Z`, `stage4-politics-20260311T064706Z`, and `stage7-politics-final-20260311T064722Z`; current result is `1 exact_hit`, `1 included_in_statistics`.
- Politics export lives at `data/exports/politics/stage4-politics-20260311T064706Z/`; the row currently has `no_public_date_evidence` and no probability model yet.
- Latest provenance seed after adding the politics slice is `stage0-provenance-20260311T064722Z`; latest Stage 8 run is `stage8-publication-20260311T064744Z`.
- Cross-family publication timing export is now `data/exports/provenance/publication-timing-audit-20260311T064801Z/` with `36` included scored rows: `30 event_precedes_publication`, `5 public_date_ok`, and `1 no_public_date_evidence`.
- Latest cohort comparison export is `data/exports/provenance/cohort-comparison-20260311T064801Z/`; current baseline cohort is `36` rows and the public-date-not-disproven cohort is `6` rows including `1` politics exact hit without public-date evidence.
- Add report `459` manual provenance evidence from the Future Of Mankind page before treating the Kevin Rudd hit as even provisionally public-date-safe; the page lists `Date of original translation: Monday, 22nd June 2020`.
- Latest manual provenance import after adding report `459` is `stage0-provenance-20260311T065818Z`; latest Stage 8 run is `stage8-publication-20260311T065836Z`.
- Latest cross-family publication timing export is `data/exports/provenance/publication-timing-audit-20260311T065901Z/`; current included scored counts are `31 event_precedes_publication` and `5 public_date_ok`.
- Latest cohort comparison export is `data/exports/provenance/cohort-comparison-20260311T065901Z/`; the public-date-not-disproven cohort is back down to `5` rows because politics `459/4` moved into `event_precedes_publication`.
- Report `459` contains additional measurable claims about Kosovo, Fidel Castro, and the USA-193 satellite shootdown that Stage 1 failed to extract; fix the date-led future-statement parser gap before expanding the politics slice further.
- Stage 1 `stage1_regex_v3` now catches date-led declarative predictions without explicit `will`; the isolated report `459` validation run `stage1-20260311T072734Z` produced `9` candidates instead of only the original `1`.
- Stage 2 `stage2_rules_v2` now normalizes `on 17 February` and `until the 21st of February`; isolated run `stage2-20260311T072746Z` recovered `3 significant_prediction` rows from report `459`: Kevin Rudd, Kosovo independence, and Fidel Castro resignation.
- Tighten declarative extraction to month-day / same-time cues only; this dropped the obvious historical false positives from `in 1963` and `in 1844` while preserving Kosovo, Castro, and the satellite shootdown row.
- Current isolated report `459` status after the parser fix: candidate `5` Kosovo is `significant_prediction`, candidate `6` Fidel Castro is `significant_prediction`, candidate `7` USA-193 is still only `prediction_but_not_meaningful` and needs rulebook tuning if we want it included.
- Expand the politics slice for recovered report `459` via `data/politics_prediction_overrides.json` and `data/politics_official_events.json`; Kosovo `459/5` and Fidel Castro `459/6` now use the same scoped government-action rulebook as Kevin Rudd `459/4`.
- Latest recovered-report politics runs are `stage3-politics-20260311T075254Z`, `stage4-politics-20260311T075302Z`, `stage7-politics-final-20260311T075309Z`, and `stage8-publication-20260311T075324Z`; the isolated slice now scores `3 exact_hit`, `3 included_in_statistics`, and `3 event_precedes_publication`.
- Rerun `scripts/export_politics_analysis.py` after Stage 8 completes; exporting in parallel with the publication review can surface stale `pending` statuses even though `prediction_audit_public_date_reviews` and the prediction rows are correct after commit.
- Politics Stage 5 now lives in `scripts/assign_politics_probabilities.py` with manual baselines in `data/politics_probability_baselines.json`; use actor/jurisdiction-specific at-risk windows when the politics slice is too narrow for a defensible family-wide event-rate catalog.
- Latest politics probability run is `stage5-politics-20260311T075725Z`; recovered report `459` probabilities are populated for all `3` rows and the claimed-date combined observed probability is `log10_sum = -7.956445`.
- Politics export `data/exports/politics/stage4-politics-20260311T075302Z/summary.json` now carries `stage5_run_key`, `p_*` fields, combined observed probability, and `public_date_cohort_summary`.
- Expand Stage 2 mechanism/consequence scoring for aviation-space action claims; adding `shot down`, `rocket`, `exploded`, `toxic fuel`, `hydrazine`, and `fall to Earth` moved report `459/7` from `prediction_but_not_meaningful` to `eligible_prediction` on rerun.
- Latest isolated report-459 Stage 2 rerun is `stage2-20260311T083824Z`; rerunning Stage 2 on the same parse run resets eligible rows in that run back to `match_status = 'unreviewed'`, so replay any family slice living on that Stage 2 key before trusting exports.
- The new aviation-space slice uses `data/aviation_space_prediction_overrides.json`, `data/aviation_space_official_events.json`, `data/aviation_space_probability_baselines.json`, and the `scripts/build|score|assign|finalize|export_aviation_space_*` pipeline files.
- Latest aviation-space runs are `stage3-aviation-space-20260311T083835Z`, `stage4-aviation-space-20260311T083847Z`, `stage5-aviation-space-20260311T083857Z`, `stage7-aviation-space-final-20260311T083857Z`, and Stage 8 `stage8-publication-20260311T083942Z`; USA-193 `459/7` is now `exact_hit`, `included_in_statistics`, `event_precedes_publication`, with claimed-date observed `log10_sum = -2.63799`.
- After the Stage 2 rerun, replay the recovered report-459 politics slice on the same key; current rerun outputs are `stage3-politics-20260311T083954Z`, `stage4-politics-20260311T084003Z`, `stage5-politics-20260311T084015Z`, `stage7-politics-final-20260311T084015Z`, and Stage 8 `stage8-publication-20260311T084015Z`.
- The coherent recovered report-459 cohort on Stage 2 run `stage2-20260311T083824Z` is now four exact hits (`459/4` through `459/7`), all with populated probability vectors and all currently `event_precedes_publication`.
- Expand the full-corpus politics slice with papal-event rows on Stage 2 run `stage2-20260310T232950Z`; current scoped rows are `113/7` (John Paul I election), `555/1` (Francis election), `829/15` (Benedict XVI death), plus `459/4` (Kevin Rudd apology).
- Keep papal-source evidence explicit: official event sources are Vatican / Holy See press pages, while public-date evidence for reports `113`, `555`, and `829` now comes from Future Of Mankind translation dates `2010-11-14`, `2020-10-19`, and `2024-12-05`.
- Latest full-corpus politics runs are `stage3-politics-20260311T084616Z`, `stage4-politics-20260311T084626Z`, `stage5-politics-20260311T084636Z`, `stage7-politics-final-20260311T084636Z`, and Stage 8 `stage8-publication-20260311T084651Z`; the scoped politics slice now has `4 exact_hit`, `4 included_in_statistics`, and `4 event_precedes_publication`.
- Rerun `export_politics_analysis.py`, `export_publication_timing_audit.py`, and `export_cohort_comparison.py` after Stage 8 completes; running them in parallel with the publication review can temporarily leave new rows at `pending` even though the DB state is already `event_precedes_publication`.
- Latest cross-family publication timing export is `data/exports/provenance/publication-timing-audit-20260311T084711Z/`; current included scored counts are `39 total`, `34 event_precedes_publication`, and `5 public_date_ok`.
- Latest cohort comparison export is `data/exports/provenance/cohort-comparison-20260311T084711Z/`; the claimed-date baseline is now `39` rows and the public-date-not-disproven cohort is back down to `5` rows because all four politics exact hits are now publication conflicts.
- Latest public-date research queue export is `data/exports/provenance/public-date-research-queue-20260311T084718Z/`; the queue now has `34` conflicts and politics `829/15` ranks ahead of most non-earthquake rows by `surprisal_log10`.
- Official FIGU German PDFs can materially improve provenance over the later English translation pages; `pdfinfo` metadata on the publicly downloadable `figu_kontaktbericht_757.pdf` and `figu_kontaktbericht_829.pdf` gave earlier provable public dates than Future Of Mankind.
- Report `757` is now rescued by official FIGU PDF metadata dated `2021-02-06`; epidemic row `757/1` moved from `event_precedes_publication` to `public_date_ok` because the matched event is `2021-02-21`.
- Report `829` now uses official FIGU PDF metadata dated `2023-01-02`; politics row `829/15` remains `event_precedes_publication`, but its publication lag dropped from `-705` days to `-2` days.
- `scripts/apply_report_provenance_evidence.py` must stay idempotent; explicit aliases are required when reading `COALESCE(...)` columns under `RealDictCursor`, otherwise every rerun re-inserts duplicate evidence rows.
- Latest clean provenance refresh after the FIGU PDF rescue batch is Stage 8 `stage8-publication-20260311T085258Z`, publication timing export `data/exports/provenance/publication-timing-audit-20260311T085353Z/`, cohort comparison `data/exports/provenance/cohort-comparison-20260311T085353Z/`, and research queue `data/exports/provenance/public-date-research-queue-20260311T085433Z/`.
- Current cross-family public-date counts are `39 included scored`, `33 event_precedes_publication`, and `6 public_date_ok`; the public-date-clean cohort now contains `6` rows, including `2` epidemic rows with `1 exact_hit`.
- Future Of Mankind MediaWiki first-revision timestamps are available at `/w/api.php`; use single-page queries because `titles=...` with `rvdir=newer` fails for multi-page requests.
- Keep mirror page-creation evidence distinct with provenance kind `wiki_first_revision`; apply `sql/20260311_prediction_audit_provenance_revision_kind.sql` before inserting those rows.
- If a provenance import fails mid-transaction, call `conn.rollback()` before updating the run row or the failure status update is ignored by PostgreSQL's aborted transaction state.
- Seed mirror revision evidence with `python3 scripts/seed_futureofmankind_revision_provenance.py --stage2-run-key stage2-20260310T232950Z`; latest live run is `stage0-provenance-20260311T091400Z` and inserted `26` first-revision evidence rows.
- Latest publication rerun after the Future Of Mankind revision import is Stage 8 `stage8-publication-20260311T091442Z`; refreshed provenance exports are `data/exports/provenance/publication-timing-audit-20260311T091442Z/`, `data/exports/provenance/cohort-comparison-20260311T091442Z/`, and `data/exports/provenance/public-date-research-queue-20260311T091442Z/`.
- The revision import tightened lags without flipping cohort counts; the closest current conflicts are now `829/15` at `-1` day and `729/12` at `-20` days.
- Full-corpus parser refresh now lives on Stage 1 `stage1-20260311T093208Z` and Stage 2 `stage2-20260311T093233Z`; the refreshed mainline counts are `6618` candidates, `25` bundles, `294` significant, and `1157` eligible.
- Compared with the older mainline Stage 2 key `stage2-20260310T232950Z`, the refreshed full cohort gained `+64` significant and `+118` eligible rows while reducing `not_a_prediction` counts by `18`.
- The mainline rerun finally includes report `459` recovery: `459/5` and `459/6` are now `significant_prediction` in `politics_election`, and `459/7` is now `eligible_prediction` in `aviation_space` on the full-corpus Stage 2 key.
- The biggest family deltas on the refreshed full-corpus Stage 2 run are `politics_election` (`21 -> 38` significant), `earthquake` (`62 -> 79` significant), `war_conflict` (`48 -> 61` significant), and `aviation_space` (`6 -> 12` significant).
- Rebuild the report-level provenance rollups after any fresh Stage 1/2 rerun; new prediction rows start with null `earliest_provable_public_date` even when `prediction_audit_report_provenance` already has evidence for those report numbers.
- Manual Stage 0 and Future Of Mankind Stage 0 importers share the same second-resolution run-key prefix; when replaying both in the same second, pass an explicit `--run-key` to avoid `prediction_audit_runs_run_key_key` collisions.
- Latest refreshed full-corpus politics replay is Stage 3 `stage3-politics-20260311T093419Z`, Stage 4 `stage4-politics-20260311T093427Z`, Stage 5 `stage5-politics-20260311T093434Z`, Stage 7 `stage7-politics-final-20260311T093441Z`, and Stage 8 `stage8-publication-20260311T093556Z`; the mainline politics slice now has `5` exact hits (`113/7`, `459/4`, `459/5`, `459/6`, `555/1`), all publication conflicts.
- Latest refreshed full-corpus aviation replay is Stage 3 `stage3-aviation-space-20260311T093419Z`, Stage 4 `stage4-aviation-space-20260311T093427Z`, Stage 5 `stage5-aviation-space-20260311T093434Z`, Stage 7 `stage7-aviation-space-final-20260311T093441Z`, and Stage 8 `stage8-publication-20260311T093556Z`; USA-193 `459/7` is now part of the main full-corpus scored cohort instead of only the isolated validation run.
- Refreshing the mainline earthquake slice on `stage2-20260311T093233Z` required new target overrides for `136:88`, `238:196`, `383:28`, `446:9`, and `453:18`, plus new final adjudications for `238:213`, `400:18`, and `779:488`.
- Latest refreshed full-corpus earthquake runs are Stage 3 `stage3-earthquake-20260311T094222Z`, Stage 4 `stage4-earthquake-20260311T094315Z`, Stage 5 `stage5-earthquake-20260311T094404Z`, Stage 6 `stage6-bundle-rollup-20260311T094518Z`, and Stage 7 `stage7-earthquake-final-20260311T094518Z`; the aligned mainline earthquake cohort is now `42 included_in_statistics`, `2 permanently_unresolved`, and `2 excluded_from_statistics`.
- Latest refreshed full-corpus epidemic runs are Stage 3 `stage3-epidemic-20260311T093807Z`, Stage 4 `stage4-epidemic-20260311T093925Z`, Stage 5 `stage5-epidemic-20260311T093932Z`, and Stage 7 `stage7-epidemic-final-20260311T094325Z`; the aligned mainline epidemic slice remains `7 included_in_statistics`.
- Do not export family or provenance summaries in parallel with `review_publication_timing.py`; rerun exports after Stage 8 completes or they will capture stale `pending` publication statuses.
- Latest fully aligned Stage 8/publication run on the refreshed mainline is `stage8-publication-20260311T094535Z`; refreshed cross-family exports are `data/exports/provenance/publication-timing-audit-20260311T094555Z/`, `data/exports/provenance/cohort-comparison-20260311T094555Z/`, and `data/exports/provenance/public-date-research-queue-20260311T094555Z/`.
- The refreshed mainline now has `55` included scored rows across `earthquake 42`, `epidemic 7`, `politics_election 5`, and `aviation_space 1`; publication timing currently splits into `41 event_precedes_publication`, `7 public_date_ok`, and `7 no_public_date_evidence`.
- The remaining `no_public_date_evidence` rows on the refreshed mainline were all earthquakes from reports `392`, `394`, `400`, `446`, `455`, and `663`; Future Of Mankind first-revision evidence exists for all of them and can be filled with `scripts/seed_futureofmankind_revision_provenance.py --report-numbers ...`.
- Latest provenance fill for that missing-earthquake batch is Stage 0 `stage0-provenance-fom-20260311T094900Z`, which inserted `6` new `wiki_first_revision` evidence rows and cleared the refreshed-mainline `no_public_date_evidence` bucket entirely.
- Latest corrected Stage 8/publication run after that fill is `stage8-publication-20260311T094904Z`; the post-Stage-8 exports are `data/exports/provenance/publication-timing-audit-20260311T094845Z/`, `data/exports/provenance/cohort-comparison-20260311T094845Z/`, and `data/exports/provenance/public-date-research-queue-20260311T094845Z/`.
- The refreshed mainline publication split is now `48 event_precedes_publication` and `7 public_date_ok`, with no remaining `no_public_date_evidence` rows.
- Post-Stage-8 provenance exports were regenerated again at `data/exports/provenance/publication-timing-audit-20260311T100839Z/`, `data/exports/provenance/cohort-comparison-20260311T100839Z/`, and `data/exports/provenance/public-date-research-queue-20260311T100839Z/`; these supersede the earlier stale export batch.
- Direct official FIGU rescue attempts for older report PDFs on `figu.org` came up empty for reports `446`, `455`, `459`, `465`, `683`, `725`, `729`, and `732`; the current working PDF path only resolves for newer reports like `757` and `829`.
- Wayback CDX queries against likely official FIGU contact-report URL patterns for reports `446`, `455`, `459`, and `465` returned no captures, so do not keep retrying the same archived-official URL patterns without a new source hint.
- Do not treat `ufofacts.co.uk/index.php/creation/goblet-of-truth` as provenance rescue evidence for report `459`; the page has no explicit publication timestamp and the earliest Wayback capture is `2019-10-18`, far too late for the 2008 politics and aviation hits.
- Cross-family provenance tools should resolve the latest completed Stage 2 run automatically instead of defaulting to the stale `stage2-20260310T232950Z` key; `scripts/provenance_export_helpers.py` now centralizes that lookup.
- Cross-family provenance exports now annotate each scored row with `current_public_source_tier`, `best_available_source_tier`, and earliest per-bucket dates; use those fields to separate mirror-backed rows from primary FIGU-supported rows before making statistical claims.
- The current refreshed mainline cohort is entirely mirror-backed at the active earliest-public-date layer; `current_public_source_bucket_counts` is `mirror: 55`, while `best_available_source_tier_counts` shows only one primary FIGU-supported scored row so far.
- Use the new `public_date_currently_unrescued` cohort and `publication_conflict_gap_bucket` field to prioritize rescue work; the latest batch is `data/exports/provenance/cohort-comparison-20260311T102931Z/` and `data/exports/provenance/public-date-research-queue-20260311T102931Z/`.
- Future Of Mankind `Stats | Source` links can expose official FIGU shop block pages even when the individual report PDF path is gone; check those source links before giving up on newer reports like `663`, `725`, `729`, and `732`.
- The canonical FIGU shop pages for Block 15 and Block 18 are `/produkt/plejadisch-plejarische-kontaktberichte-block-15/` and `/produkt/plejadisch-plejarische-kontaktberichte-block-18/`; Block 18 uses the correct `kontaktberichte` spelling even though Future Of Mankind links the misspelled `kontakberichte` slug.
- FIGU shop block pages currently expose `article:published_time` / `datePublished` only from `2022-10-21`, so they add primary-source support for reports `663`, `725`, `729`, and `732` but do not rescue any current publication conflicts.
- After importing the FIGU shop block-page evidence, the refreshed mainline `best_available_source_tier_counts` moved from `official_figu_original: 1` to `official_figu_original: 5`; use `data/exports/provenance/cohort-comparison-20260311T103323Z/` and `data/exports/provenance/public-date-research-queue-20260311T103323Z/` as the superseding batch.
- Use `scripts/seed_figu_shop_provenance.py` to bulk-seed official FIGU shop evidence from Future Of Mankind `Source` links; cache shop-page fetches by normalized URL or the run becomes painfully slow because many reports share the same block product page.
- The FIGU shop importer should reject block mismatches instead of silently following redirects; report `519` links `Block 12`, but the current shop path redirects to `Block 13`, so treat that as non-defensible until a true Block 12 source appears.
- The bulk FIGU shop seed run `stage0-provenance-20260311T104855Z` inserted `30` official-source evidence rows across the `31` scored reports and moved the refreshed mainline `best_available_source_tier_counts` to `official_figu_original: 54, mirror_translation_page: 1`.
- After the full FIGU shop import, all `7` public-date-clean scored rows are now `primary_supported_clean`; use `data/exports/provenance/cohort-comparison-20260311T105058Z/` and `data/exports/provenance/public-date-research-queue-20260311T105058Z/` as the new provenance baseline.
- Use `scripts/export_prediction_audit_overview.py` for the top-line morning numbers; the first overview export is `data/exports/overview/prediction-audit-overview-20260311T105316Z/` and rolls parse counts, scored counts, public-date-clean hits, and provenance-tier counts into one summary.
- Resolve `main -> main (fetch first)` push rejections by fetching and merging `origin/main` into local `main` when the remote only adds site-content commits; latest clean integration commit is `db265d8` and leaves the branch `ahead 41`.
- Earthquake Stage 3 should not hard-require parser-populated `time_window_start/time_window_end`; honor per-prediction override windows from `data/earthquake_prediction_overrides.json`, persist them back to `prediction_audit_predictions`, and then rerun Stage 3/4/5/7.
- Latest widened earthquake replay is Stage 3 `stage3-earthquake-20260311T210822Z`, Stage 4 `stage4-earthquake-20260311T210911Z`, Stage 5 `stage5-earthquake-20260311T210920Z`, Stage 6 `stage6-bundle-rollup-20260311T211037Z`, and Stage 7 `stage7-earthquake-final-20260311T211043Z`; aligned mainline earthquake cohort is now `45 included_in_statistics`, `2 permanently_unresolved`, and `2 excluded_from_statistics`.
- New earthquake override-driven additions on the refreshed mainline are `400/7` exact hit, `402/5` exact hit, and `402/6` similar-only; refreshed top-line overview is `data/exports/overview/prediction-audit-overview-20260311T211058Z/` with `58` included scored rows and `33` exact hits.
- `scripts/seed_futureofmankind_revision_provenance.py --report-numbers` expects a single comma-separated value like `395,408`, not space-separated arguments.
- When replaying Stage 8 after provenance fills, rerun the provenance and overview exporters sequentially after `review_publication_timing.py` completes; parallel exports can snapshot stale `no_public_date_evidence` rows.
- Latest widened earthquake replay is Stage 3 `stage3-earthquake-20260311T220747Z`, Stage 4 `stage4-earthquake-20260311T220843Z`, Stage 5 `stage5-earthquake-20260311T220849Z`, Stage 6 `stage6-bundle-rollup-20260311T221005Z`, and Stage 7 `stage7-earthquake-final-20260311T221005Z`; aligned mainline earthquake cohort is now `51 included_in_statistics`, `2 permanently_unresolved`, and `2 excluded_from_statistics`.
- New second-batch earthquake additions on the refreshed mainline are `395/4` exact hit, `402/3` near hit, `408/5` exact hit, `446/6` exact hit, `446/8` similar-only, and `519/1` exact hit; refreshed top-line overview is `data/exports/overview/prediction-audit-overview-20260311T221113Z/` with `64` included scored rows and `37` exact hits.
- Latest widened earthquake replay is Stage 3 `stage3-earthquake-20260311T221559Z`, Stage 4 `stage4-earthquake-20260311T221652Z`, Stage 5 `stage5-earthquake-20260311T221657Z`, Stage 6 `stage6-bundle-rollup-20260311T221825Z`, and Stage 7 `stage7-earthquake-final-20260311T221825Z`; aligned mainline earthquake cohort is now `54 included_in_statistics`, `2 permanently_unresolved`, and `2 excluded_from_statistics`.
- New third-batch earthquake additions on the refreshed mainline are `401/2` exact hit, `408/7` near hit, and `427/1` exact hit; refreshed top-line overview is `data/exports/overview/prediction-audit-overview-20260311T221916Z/` with `67` included scored rows and `39` exact hits.
- Politics widening reuses the existing override-driven pipeline cleanly; add new rows by patching `data/politics_prediction_overrides.json`, `data/politics_official_events.json`, and `data/politics_probability_baselines.json`, then replay Stage 3/4/5/7 on the aligned Stage 2 key.
- The refreshed mainline politics replay `stage3-politics-20260311T223432Z` / `stage4-politics-20260311T223437Z` / `stage5-politics-20260311T223443Z` / `stage7-politics-final-20260311T223448Z` expanded politics from `5` to `10` exact hits by adding `113/11`, `136/16`, `136/52`, `217/3`, and `448/7`.
- Seed Future Of Mankind revision provenance for newly scored reports before rerunning Stage 8; the new politics reports `217` and `448` were filled with `stage0-provenance-20260311T223500Z`.
- After the widened politics replay, rerun Stage 8 and the downstream exporters sequentially; aligned outputs are `publication-timing-audit-20260311T223518Z`, `cohort-comparison-20260311T223521Z`, `public-date-research-queue-20260311T223526Z`, and `prediction-audit-overview-20260311T223533Z`.
- Report `436/11` fits the politics slice cleanly as a party-control event; model it as Republicans losing the U.S. House majority on `2006-11-07` and source it from `history.house.gov` rather than a subjective news recap.
- The follow-up politics replay `stage3-politics-20260311T223812Z` / `stage4-politics-20260311T223815Z` / `stage5-politics-20260311T223824Z` / `stage7-politics-final-20260311T223829Z` expanded politics from `10` to `11` exact hits.
- Aligned post-436 exports are `publication-timing-audit-20260311T223843Z`, `cohort-comparison-20260311T223853Z`, `public-date-research-queue-20260311T223856Z`, and `prediction-audit-overview-20260311T223900Z`; top-line mainline is now `73` included scored rows with `45` exact hits.
- The copied politics slice scripts can bootstrap a new narrow family quickly; rename the family metadata, default data paths, run-key prefixes, and rationale strings before running `py_compile` or Stage 3.
- The first storm slice uses `448/6` only; `stage3-storm-20260311T224310Z` / `stage4-storm-20260311T224316Z` / `stage5-storm-20260311T224323Z` / `stage7-storm-final-20260311T224328Z` scored the Greensburg tornado row as an `exact_hit`.
- Aligned post-storm exports are `publication-timing-audit-20260311T224350Z`, `cohort-comparison-20260311T224356Z`, `public-date-research-queue-20260311T224401Z`, and `prediction-audit-overview-20260311T224407Z`; top-line mainline is now `74` included scored rows with `46` exact hits.
- The first volcano slice can also be bootstrapped from the copied storm scripts; `stage3-volcano-20260311T225046Z` / `stage4-volcano-20260311T225052Z` / `stage5-volcano-20260311T225101Z` / `stage7-volcano-final-20260311T225110Z` scored `150:133`, `238:7`, `383:27`, and `444:3`.
- Curated family overrides should be allowed to score rows even when Stage 2 was conservative; the volcano scripts now fetch override-key rows regardless of `stage2_label`, which let `464:4` Chaiten be included without a full Stage 2 replay.
- After adding Chaiten and filling reports `444` and `464` with Future Of Mankind revision provenance, the final aligned exports are `publication-timing-audit-20260311T225355Z`, `cohort-comparison-20260311T225407Z`, `public-date-research-queue-20260311T225415Z`, and `prediction-audit-overview-20260311T225422Z`.
- The current aligned mainline is `79` included scored rows with `50` exact hits, `15` near hits, `8` similar-only, and `6` misses; the public-date split is `72 event_precedes_publication` and `7 public_date_ok`.
- Search pre-2020 corona claims with targeted disease terms, not plain `crown`; the earliest explicit corona wording in the corpus is report `341` on `2003-04-02`, which says the pathogen is a `corona virus mutation`.
- Epidemic override slices must ignore conservative `stage2_label` filters the same way volcano does; removing the eligible/significant-only gate let `341:15` score cleanly as the SARS coronavirus confirmation row without a full Stage 2 rerun.
- Report `341/15` is now the earliest scored corona-family row on the refreshed mainline; the aligned exports after the epidemic replay are `publication-timing-audit-20260311T230840Z`, `cohort-comparison-20260311T230843Z`, `public-date-research-queue-20260311T230847Z`, and `prediction-audit-overview-20260311T230848Z`.
- The epidemic catalog can widen cleanly with disease-specific, sourceable rows even when Stage 2 was conservative; `215/69` Ebola, `364/17` West Nile, and `383/32` H2N2 all scored as exact hits on the refreshed mainline without any parser rerun.
- After the widened epidemic replay, the aligned exports are `publication-timing-audit-20260311T231530Z`, `cohort-comparison-20260311T231535Z`, `public-date-research-queue-20260311T231539Z`, and `prediction-audit-overview-20260311T231543Z`; mainline is now `83` scored with `54` exact hits.
